define(function(require, exports, module) {
    require('dm.modal');
    require('dm.ajaxpost');
    require('dm.validate');
    
    $(function(){
      

      /*// 同步
      $(document).off('submit.native').on('submit.native', '#investor-app', function (e, eventData) {
          e.preventDefault()
          var $form = $(this)
          console.log("000000");
          $form.validate(null, function(){
              // 没有错误则同步提交
              $form[0].submit();
          });
          
      });*/
  });

    
});